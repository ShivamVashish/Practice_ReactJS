<!-- Q: Why do we use data.items with GitHub search API?
A: Because the search API returns an object containing metadata and the actual user list inside the items array.

Why do we use useCallback in React?

useCallback is used to stop a function from being created again and again on every render.
It saves memory and improves performance by keeping the same function reference unless its dependencies change. -->