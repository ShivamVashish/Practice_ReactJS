<!-- 🔹 Easy Interview Lines -->
<!-- 
useState → “Stores data for UI and updates component when state changes.”

useEffect → “Runs a function after render, only if dependencies change.”

useCallback → “Remembers a function, so it won’t be recreated unless needed.”

useMemo → “Remembers a calculated value, recalculates only if needed.” 

